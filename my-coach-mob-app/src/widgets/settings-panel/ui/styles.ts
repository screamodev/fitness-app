import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 5,
  },
  card: {
    borderRadius: 8,
    overflow: 'hidden',
    marginBottom: 20,
  },
});
