import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputWrapper: {
    width: '100%',
  },
  inputContainer: {
    marginTop: 0,
  },
  input: {
    paddingLeft: 0,
    paddingRight: 0,
  },
});
