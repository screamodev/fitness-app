import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  controlButton: {
    position: 'absolute',
    top: 9,
    zIndex: 100,
  },
  leftControlButton: {
    left: -10,
  },
  rightControlButton: {
    right: -10,
  },
});
