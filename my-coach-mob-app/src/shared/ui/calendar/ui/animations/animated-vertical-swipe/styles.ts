import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'relative',
    display: 'flex',
    overflow: 'hidden',
    paddingBottom: 30,
    justifyContent: 'flex-start',
  },
});
