import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  swipeBar: {
    position: 'absolute',
    bottom: 14,
    width: 38,
    height: 2,
    borderRadius: 10,
    alignSelf: 'center',
  },
});
