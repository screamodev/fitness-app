import { MaterialCommunityIcons } from '@expo/vector-icons';

import { IconProps } from 'shared/config/icon';

export function CrossIcon(props: IconProps) {
  return <MaterialCommunityIcons name="window-close" {...props} />;
}
