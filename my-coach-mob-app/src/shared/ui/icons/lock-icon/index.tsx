import { MaterialCommunityIcons } from '@expo/vector-icons';

import { IconProps } from 'shared/config/icon';

export function LockIcon(props: IconProps) {
  return <MaterialCommunityIcons name="lock-outline" {...props} />;
}
