import { MaterialCommunityIcons } from '@expo/vector-icons';

import { IconProps } from 'shared/config/icon';

export function PlusIcon(props: IconProps) {
  return <MaterialCommunityIcons name="plus" {...props} />;
}
