import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    paddingBottom: 40,
  },
  content: {
    justifyContent: 'space-around',
    alignItems: 'center',
    flex: 1,
  },
});
