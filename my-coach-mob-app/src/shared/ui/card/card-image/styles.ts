import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 70,
    height: 68,
    borderRadius: 12,
    overflow: 'hidden',
  },
  image: {
    height: '100%',
    width: '100%',
  },
});
