import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 10,
    minHeight: 80,
    borderRadius: 12,
    padding: 6,
  },
  containerContent: {
    flex: 1,
    alignItems: 'center',
    flexDirection: 'row',
  },
  content: {
    flex: 1,
    marginLeft: 12,
  },
});
