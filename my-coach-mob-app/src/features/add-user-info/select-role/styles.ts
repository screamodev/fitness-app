import { StyleSheet } from 'react-native';

import { Layout } from 'shared/config/layout';

export const styles = StyleSheet.create({
  content: {
    maxWidth: Layout.width.lg,
    width: '100%',
    alignItems: 'center',
  },
});
