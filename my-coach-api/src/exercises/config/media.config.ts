import { IMediaConfig } from 'medias/interfaces/media-config.interface';

export const mediaConfig: IMediaConfig = {
  mediaRepository: 'exerciseMedia',
  field: 'exerciseId',
  selectedMediaField: 'primaryImageId',
  repository: 'exercise',
};
